/* Initials Entry Screen Stylesheet */

#prompt-text,
#confirm-prompt {
    font-size: 20px;
    color: rgba(255, 255, 255, 1);
    /* TODO: replace -unity-font with Press Start 2P TMP font asset when available */
    -unity-font: resource('Fonts & Materials/LegacyRuntime');
}
